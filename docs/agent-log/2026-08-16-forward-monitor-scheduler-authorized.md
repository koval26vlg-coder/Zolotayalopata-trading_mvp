# 2026-08-16 — forward-тик по расписанию авторизован пользователем

Пользователь явно разрешил автоматический scheduler-тик (план
`slow_liquidity_listing_momentum_forward_monitor_20260816` запрещал
scheduler без явного разрешения — теперь оно дано):

- фраза: «поставить автоматический тик по расписанию каждые 6 часов
  видимым запуском»
- automation id: `automation-66009175-62c6-4dbc-8631-bae668b9cad9`
- cron `0 */6 * * *` (местное время 00:00/06:00/12:00/18:00), recurring
- каждый fire: один видимый тик через
  `tools/start_listing_momentum_forward_tick_visible.ps1` (launcher сам
  проверяет gate/claim/prefetch-хэши и открывает видимое окно);
  при блокировке gate — только фиксация, без форса; один тик на
  срабатывание, без повторов при сбое
- план (`plan_hash=bc55d56f…`) не изменялся; scheduler-разрешение
  лежит поверх плана как пользовательская авторизация cadence
- ограничение: срабатывание требует включённой машины; пропущенный fire
  не критичен (окно листинга 3 дня, следующий тик доберёт)

## Next
Тики копят complete-окна в forward state
(`slow_liquidity_listing_momentum_forward_state_20260816.json`);
при достаточной выборке — отдельный evaluator-план.
